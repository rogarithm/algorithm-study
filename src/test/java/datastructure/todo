todo
 문자 하나를 trie에 저장할 수 있다
  첫 문자로 나올 수 있는 문자는 여러가지일 수 있다. a-..., b-..., ...
 문자 여러개를 trie에 저장할 수 있다
  trie는 등록한(addWord) 단어를 가지고 있어야 한다
   TreeNode는 left,right만 자식으로 가질 수 있다. 하지만 한 문자 뒤에 나올 수 있는 경우는 2가지 이상이 될 수 있다.
   어떻게 해결하는 게 좋을까?
  한 문자에 연결할 수 있는 문자가 여러가지일 수 있다. a-b, a-c, ...
 여러 단어가 trie에 저장되었을 때, 공통되는 부분은 한 번만 저장한다
 단어 끝 표시를 할 수 있다